# 文件路径: simple-lama-inpainting-main/yolo/watermark.yaml
path: yolo/dataset           # 数据集根目录（相对路径）

train: images/train     # 训练图像文件夹（相对 path）
val: images/val         # 验证图像文件夹（可以先用相同目录）

names:
  0: watermark          # 你的类别名，对应标注文件中的 class_id（从 0 开始）
